import React, { useState, useEffect } from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
import {colors, commonStyles} from '../../styles/commonStyles';

const AllTownsComponent = ({ userId }) => {


    return (
        <View style={commonStyles.contentContainer}>
            {/* TODO: Map all towns to a view, with either only a view button (and inside the town they can hit join, or view/join as two buttons on each view) */}
            <Text>hi</Text>
        </View>
    );
}   

export default AllTownsComponent;